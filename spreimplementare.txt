

##002 input pin to reveal CNP

server will store information in redis 

container.buffers = {
    {bufferitem-title}: {bufferid} 
}

admin:{userid}:securitybuffers:{bufferid}:information EX 60 * 15 (15 minutes)
64 chars

##004 replace 401 with 403 where logical

401 unathorized (because authentication error)
403 forbidden 

##005 add  wherever possible

##006 in >admin.js

##007 id fail -> displayNames fallback

##008 Check and correct wrong validator string lengths